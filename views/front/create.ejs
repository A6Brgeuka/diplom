<% layout('layout/page') -%>
<% block('title', "Create") -%>

<p>Page to create advert</p>

<form class="form-horizontal login-form" name="create-form" action="/create" method="post">
    <div class="form-group">
        <label for="input-title" class="col-lg-2 control-label">title</label>
        <div class="col-lg-10">
            <input name="title" value="title" type="text" class="form-control" id="input-title" placeholder="title">
        </div>
    </div>
    <div class="form-group">
        <label for="input-name" class="col-lg-2 control-label">name</label>

        <div class="col-lg-10">
            <input name="name" value="name" type="text" class="form-control" id="input-name" placeholder="name">
        </div>
    </div>
    <div class="form-group">
        <label for="input-shortDescription" class="col-lg-2 control-label">shortDescription</label>

        <div class="col-lg-10">
            <textarea name="shortDescription" class="form-control" id="input-shortDescription" placeholder="shortDescription">shortDescription</textarea>
        </div>
    </div>
    <div class="form-group">
        <label for="input-longDescription" class="col-lg-2 control-label">longDescription</label>

        <div class="col-lg-10">
            <textarea name="longDescription" class="form-control" id="input-longDescription" placeholder="longDescription">longDescription</textarea>
        </div>
    </div>
    <div class="form-group">
        <div class="col-lg-offset-2 col-lg-10">
            <button type="submit" class="btn btn-primary" data-loading-text="Sending...">Add</button>
            <span class="help-block error"></span>
        </div>
    </div>
</form>


<!--
<script>
    $(document.forms['create-form']).on('submit', function() {
        var form = $(this);

        $('.error', form).html('');
        $(":submit", form).button("loading");

        $.ajax({
            url: "/create",
            method: "POST",
            data: form.serialize(),
            complete: function() {
                $(":submit", form).button("reset");
            },
            statusCode: {
                200: function() {
                    form.html("Advert created").addClass('alert-success');
                    //window.location.href = "/";
                },
                403: function(jqXHR) {
                    var error = JSON.parse(jqXHR.responseText);
                    $('.error', form).html(error.message);
                }
            }
        });
        return false;
    });
</script>-->
